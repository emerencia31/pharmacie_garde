{% extends 'base.html.twig' %}

{% block title %}Commune{% endblock %}

{% block body %}
    <h1>Commune</h1>

    <table class="table table-striped table-hover mb-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commune.id }}</td>
            </tr>
            <tr>
                <th>Nom_com</th>
                <td>{{ commune.nomCom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commune_index') }}" class="btn btn-secondary">Retour à la liste</a><br><br>

    <a href="{{ path('commune_edit', {'id': commune.id}) }}" class="btn btn-primary">modifier</a><br><br>

    {{ include('commune/_delete_form.html.twig') }}
{% endblock %}
