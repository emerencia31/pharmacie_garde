{% extends 'base.html.twig' %}

{% block title %}Region{% endblock %}

{% block body %}
    <h1>Region</h1>

    <table class="table table-striped table-hover mb-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ region.id }}</td>
            </tr>
            <tr>
                <th>Nom_reg</th>
                <td>{{ region.nomReg }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('region_index') }}" class="btn btn-secondary">Retour à la liste</a><br><br>

    <a href="{{ path('region_edit', {'id': region.id}) }}" class="btn btn-primary">modifier</a><br><br>

    {{ include('region/_delete_form.html.twig') }}
{% endblock %}
