{% extends 'base.html.twig' %}

{% block title %}Responsable{% endblock %}

{% block body %}
    <h1>Responsable</h1>

    <table class="table table-striped table-hover mb-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ responsable.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ responsable.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ responsable.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('responsable_index') }}" class="btn btn-secondary">Retour à la liste</a><br><br>

    <a href="{{ path('responsable_edit', {'id': responsable.id}) }}" class="btn btn-primary">modifier</a><br><br>

    {{ include('responsable/_delete_form.html.twig') }}
{% endblock %}
