{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>

    <table class="table table-striped table-hover mb-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom_ville</th>
                <td>{{ ville.nomVille }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ville_index') }}" class="btn btn-secondary">Retour à la liste</a><br><br>

    <a href="{{ path('ville_edit', {'id': ville.id}) }}" class="btn btn-primary">modifier</a><br><br>

    {{ include('ville/_delete_form.html.twig') }}
{% endblock %}
