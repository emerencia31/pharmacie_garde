{% extends 'base.html.twig' %}

{% block title %}Garde{% endblock %}

{% block body %}
    <h1>Garde</h1>

    <table class="table table-striped table-hover mb-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garde.id }}</td>
            </tr>
            <tr>
                <th>Nom_garde</th>
                <td>{{ garde.nomGarde }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('garde_index') }}" class="btn btn-secondary">Retour à la liste</a><br><br>

    <a href="{{ path('garde_edit', {'id': garde.id}) }}" class="btn btn-primary">modifier</a><br><br>

    {{ include('garde/_delete_form.html.twig') }}
{% endblock %}
